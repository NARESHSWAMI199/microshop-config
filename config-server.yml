server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nareshswami/microshop-config.git
          default-label: main
eureka:
  client:
    enabled: false
