server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NARESHSWAMI199/microshop-config.git
          default-label: main
eureka:
  client:
    register-with-eureka: false 
    fetch-registry: false #
